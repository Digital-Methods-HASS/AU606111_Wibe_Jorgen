# Counting the sum of numbers greater than two excluding the NA's
length(!is.na(rooms) > 2)
rooms <- c(1, 2, 4, 5, 1, 3, 1, NA, 3, 1, 3, 2, 1, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 2, 1, 7, 1, 9, 3, NA)
# Counting the sum of numbers greater than two excluding the NA's
length(!is.na(rooms) > 2)
# Counting the sum of numbers greater than two excluding the NA's
length(!is.na(rooms) < 2)
# Counting the sum of numbers greater than two excluding the NA's
length(na.omit(rooms) > 2)
sum(na.omit(rooms) > 2)
# Counting the sum of numbers greater than two excluding the NA's
rooms <- na.omit(rooms)
length(rooms < 2)
length(rooms > 2)
length(na.omit(rooms) > 2)
rooms <- c(1, 2, 4, 5, 1, 3, 1, NA, 3, 1, 3, 2, 1, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 2, 1, 7, 1, 9, 3, NA)
# Counting the sum of numbers greater than two excluding the NA's
length(na.omit(rooms) > 2)
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms) > 2)
length(rooms)
length(rooms == 2)
rooms == 2
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms) > 2)
rooms == 2
na.omit(rooms) == 2
na.omit(rooms) == 2
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms) > 2)
na.omit(rooms) == 2
# Counting how many elements are greater than two excluding the NA's
sum(na.omit(rooms, na.rm = TRUE) > 2)
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms, na.rm = TRUE) > 2)
# Counting how many elements are greater than two excluding the NA's
sum(na.omit(rooms, na.rm = TRUE) > 2)
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms, na.rm = TRUE) > 2)
# Counting how many elements are greater than two excluding the NA's
sum(na.omit(rooms) > 2)
pacman::p_load(tidyverse)
knitr::opts_chunk$set(echo = TRUE)
library(pacman)
pacman::p_load(tidyverse)
rooms <- c(1, 2, 4, 5, 1, 3, 1, NA, 3, 1, 3, 2, 1, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 2, 1, 7, 1, 9, 3, NA)
# Counting how many elements are greater than two excluding the NA's
rooms %>%
na.omit(rooms) %>%
length(rooms > 2)
# Counting how many elements are greater than two excluding the NA's
rooms %>%
na.omit(rooms) %>%
sum(rooms > 2)
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms) > 2)
length(rooms > 2)
rooms > 2
rooms > 2, na.rm=T
rooms, na.rm=T > 2
length(rooms[rooms > 2])
# Counting how many elements are greater than two excluding the NA's
length(na.omit[rooms > 2])
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms)[rooms > 2])
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms) > 2)
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms)[rooms > 2] > 2)
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms)[rooms > 2])
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms)[rooms > 2])
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms)[rooms > 2])
# Counting the sum of numbers greater than two excluding the NA's
sum(rooms>2)
# Counting the sum of numbers greater than two excluding the NA's
sum(rooms[rooms>2])
# Counting the sum of numbers greater than two excluding the NA's
sum(rooms[rooms>2], rm.na=T)
# Counting the sum of numbers greater than two excluding the NA's
sum(rooms[rooms>2])
# Counting the sum of numbers greater than two excluding the NA's
sum(rooms)
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms))
test <- rooms>2
test <- rooms[rooms==2]
rooms[rooms==2]
na.omit(rooms[rooms==2])
# removing NA's
na.omit(rooms)
rooms <- c(1, 2, 4, 5, 1, 3, 1, NA, 3, 1, 3, 2, 1, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 2, 1, 7, 1, 9, 3, NA)
# removing NA's
na.omit(rooms)
# removing NA's
!is.na(rooms)
# removing NA's
rooms <- omit.na(rooms)
rooms <- c(1, 2, 4, 5, 1, 3, 1, NA, 3, 1, 3, 2, 1, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 2, 1, 7, 1, 9, 3, NA)
# removing NA's
rooms <- na.omit(rooms)
rooms[rooms==2]
# Counting the sum of numbers greater than two excluding the NA's
sum(rooms[rooms > 2])
rooms <- c(1, 2, 4, 5, 1, 3, 1, NA, 3, 1, 3, 2, 1, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 2, 1, 7, 1, 9, 3, NA)
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms)[rooms > 2])
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms[rooms > 2]))
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms)[rooms > 2])
rm(test)
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms)[rooms > 2])
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms)[rooms > 2]))
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms[rooms > 2]))
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms[rooms > 2]))
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms[rooms > 2]))
* Sum of elements greater than two = **55**
tinytex::install_tinytex()
setwd("~/Desktop/CogSci/Cultural data science/AU606111_Wibe_Jorgen/HW3")
# Chunk 1: setup
knitr::opts_chunk$set(echo = TRUE)
library(pacman)
pacman::p_load(tidyverse)
# Chunk 2
rooms <- c(1, 2, 4, 5, 1, 3, 1, NA, 3, 1, 3, 2, 1, NA, 1, 8, 3, 1, 4, NA, 1, 3, 1, 2, 1, 7, 1, 9, 3, NA)
# Counting how many elements are greater than two excluding the NA's
length(na.omit(rooms[rooms > 2]))
# Counting the sum of numbers greater than two excluding the NA's
sum(na.omit(rooms[rooms > 2]))
# Chunk 3
class(rooms)
# Chunk 4
library(tidyverse)
interviews <- read_csv("/Users/WIBE/Desktop/CogSci/Cultural data science/Introduction course/Day 2/data/SAFI_clean.csv")
# Chunk 5
kings_and_queens <- read_csv(as_tibble("/Users/WIBE/Desktop/CogSci/Cultural data science/Introduction course/Day 2/data/Danish Monarchs - Sheet1.csv"))
# Chunk 6
median((kings_and_queens$end_reign)-(kings_and_queens$start_reign), na.rm = T)
mean((kings_and_queens$end_reign)-(kings_and_queens$start_reign), na.rm = T)
kings_and_queens %>%
mutate(reign_length = end_reign-start_reign) %>%
filter(reign_length > 42) %>%
select(Monarch,reign_length)
kings_and_queens %>%
mutate(reign_length = end_reign-start_reign) %>%
arrange(reign_length) %>%
select(Monarch,reign_length)
kings_and_queens %>%
mutate(reign_length = end_reign-start_reign) %>%
arrange(reign_length) %>%
select(Monarch,reign_length) %>%
tail(3)
kings_and_queens %>%
mutate(reign_length = end_reign-start_reign) %>%
arrange(reign_length) %>%
select(Monarch,reign_length) %>%
tail(4)
kings_and_queens %>%
mutate(reign_length = end_reign-start_reign) %>%
arrange(desc(reign_length)) %>%
select(Monarch,reign_length) %>%
head(4)
kings_and_queens %>%
mutate(reign_length = end_reign-start_reign) %>%
arrange(desc(reign_length)) %>%
select(Monarch,reign_length) %>%
head(3)
kings_and_queens %>%
mutate(reign_length = end_reign-start_reign) %>%
arrange(desc(reign_length)) %>%
select(Monarch,reign_length) %>%
head(3)
